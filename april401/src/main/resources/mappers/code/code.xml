<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.april.groupware.code">

	<!-- 목록 조회 -->
	<select id="doRetrieve" parameterType="CodeVO" resultType="CodeVO">
		SELECT t2.code_id AS codeId, 
			t2.code_nm AS codeNm,
			t2.seq
		FROM code_type t1, code t2
		WHERE t1.code_type_id = t2.code_type_id
		AND t1.code_type_id = #{codeTypeId}
		AND t2.use_yn = '1'
		ORDER BY t2.seq asc
	</select>
	
</mapper>